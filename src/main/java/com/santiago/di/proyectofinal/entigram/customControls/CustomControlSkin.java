package com.santiago.di.proyectofinal.entigram.customControls;

import javafx.scene.control.Control;
import javafx.scene.control.SkinBase;

public class CustomControlSkin extends SkinBase<Control> {

    public CustomControlSkin(Control control) {
        super(control);
        // Aquí puedes definir la apariencia y la lógica de interacción para tu control
        // Por ejemplo, crear nodos gráficos y configurar su comportamiento
    }
}